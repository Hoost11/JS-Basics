function vacationBooks (input){
let bookPages = Number(input[0])
let pagesForOneHour = Number(input[1])
let daysNeeded = Number(input[2])
let hoursNeeded = (Number(input[0]) / Number(input[1])) / 2

console.log(hoursNeeded)
}

vacationBooks(["212", "20", "2"])